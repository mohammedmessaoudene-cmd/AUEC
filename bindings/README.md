# Separable bindings boundary

Future transport/interface bindings that do not import runtime core logic may
be placed here under Apache-2.0. The existing
`reference-runtime/aiew_uc/bindings.py` is runtime-coupled and remains
AGPL-3.0-only; it is not relabeled merely because its filename says
"bindings".
