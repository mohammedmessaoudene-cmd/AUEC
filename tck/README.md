# AUEC conformance material

This Apache-2.0 directory is reserved for transport-neutral vectors and
conformance logic that can be used by independent implementations. It must not
import the AGPL reference runtime.

The current public smoke tests under `tests/` exercise the reference runtime
and are therefore AGPL-3.0-only. They are not represented as an independent
TCK.
